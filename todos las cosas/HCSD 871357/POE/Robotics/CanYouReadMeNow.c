//The Hopper

task main()
{
	while(1==1)
	{
		untilSonarLessThan(3);
		{
			setServo(servo, 120);
			if(SensorValue[lightSensor]<value);
			{
				woodMarbleCount=woodMarbleCount+1
			}
			if (SensorValue[lightSensor]
			wait(3);
		}
	}
}
